# Local-dev-only Mosquitto config for the modules/iot demo — anonymous
# access, no TLS. Do not use this configuration as-is in production; add
# authentication (password_file) and a TLS listener before exposing this
# broker outside a trusted Docker network.
listener 1883
allow_anonymous true

persistence true
persistence_location /mosquitto/data/
log_dest stdout
