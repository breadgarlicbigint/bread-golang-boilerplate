# .air.worker.toml — hot reload config for the Redis/Asynq worker (apps/worker)
# https://github.com/air-verse/air

root = "."
tmp_dir = ".air-worker"

[build]
  cmd = "go build -o .air-worker/worker ./apps/worker"
  bin = ".air-worker/worker"
  full_bin = ".air-worker/worker"
  include_ext = ["go", "tpl", "tmpl", "html"]
  exclude_dir  = ["assets", "tmp", ".air", ".air-worker", ".air-worker-rabbitmq", ".air-worker-kafka", "vendor", "testdata", "docs", "web"]
  exclude_file = []
  exclude_regex = ["_test.go"]
  exclude_unchanged = true
  follow_symlink = false
  log = ".air-worker/build.log"
  delay = 1000          # ms
  stop_on_error = true
  send_interrupt = false
  kill_delay = "0s"

[color]
  main    = "magenta"
  watcher = "cyan"
  build   = "yellow"
  runner  = "green"

[log]
  time = false

[misc]
  clean_on_exit = true
